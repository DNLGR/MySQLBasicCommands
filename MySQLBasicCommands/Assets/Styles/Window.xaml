<ResourceDictionary     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:local="clr-namespace:MySQLBasicCommands"
                        xmlns:system="clr-namespace:System;assembly=mscorlib">

    <Style x:Key="modern_window" TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{x:Null}" />
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="ShowInTaskbar" Value="True" />
        <Setter Property="AllowsTransparency" Value="True" />
        <Setter Property="ResizeMode" Value="NoResize" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border
                        CornerRadius="15"
                        Margin="5"
                        BorderBrush="{DynamicResource primary_brush}"
                        BorderThickness="0.5"
                        Background="{DynamicResource primary_brush}">
                        <Border.Effect>
                            <DropShadowEffect Direction="-75" ShadowDepth="0" Opacity="0.3" BlurRadius="10" Color="Black"/>
                        </Border.Effect>
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>